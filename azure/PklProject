amends "pkl:Project"

dependencies {
  ["formae"] {
    uri = "package://hub.platform.engineering/plugins/pkl/schema/pkl/formae/formae@0.89.0"
  }

  // appgw mode needs Network::ApplicationGateway + KeyVault::Certificate +
  // ManagedIdentity + role assignment. ApplicationGateway and KeyVault::Certificate
  // landed in 0.1.10, NOT 0.1.8 as previously pinned — on 0.1.8 `--access appgw`
  // could not even evaluate ("Cannot find module .../keyvault/certificate.pkl").
  // public/tailnet work with any 0.1.x.
  ["azure"] {
    uri = "package://hub.platform.engineering/plugins/azure/schema/pkl/azure/azure@0.1.14"
  }
}
