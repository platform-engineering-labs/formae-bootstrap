# Self-signed TLS material for the agent ALB (generate locally with openssl; see README).
# Never commit private keys or environment-specific certs.
cert.key
cert.crt

# Generated by `pkl project resolve`; ignored in gcp/ and azure/ too.
PklProject.deps.json
